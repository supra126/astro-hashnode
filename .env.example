# (Required) Your Hashnode publication host
# Find it at: Hashnode Dashboard → your blog URL (e.g. yourblog.hashnode.dev)
PUBLIC_HASHNODE_PUBLICATION_HOST=engineering.hashnode.com

# (Optional) Site language — BCP 47 code (e.g. en, zh-TW, ja, ko)
# Affects <html lang>, date formatting, and Giscus UI language
# PUBLIC_SITE_LANG=en

# (Optional) Site URL — set this if you deploy to a custom domain
# Auto-detected on Vercel via VERCEL_PROJECT_PRODUCTION_URL
# Defaults to https://{PUBLIC_HASHNODE_PUBLICATION_HOST}
# Used for canonical URLs, sitemap, and analytics
# PUBLIC_SITE_URL=https://blog.example.com

# (Optional) Hashnode GraphQL API endpoint — override only if you use a proxy or custom endpoint
# PUBLIC_HASHNODE_GQL_ENDPOINT=https://gql.hashnode.com

# (Optional) Giscus Comments — powered by GitHub Discussions (https://giscus.app)
# Set to "true" to replace Hashnode's read-only comments with interactive Giscus comments
# All four values must be set for Giscus to activate; otherwise falls back to Hashnode comments
# Visit https://giscus.app to generate the values below
# PUBLIC_GISCUS_ENABLED=true
# PUBLIC_GISCUS_REPO=your-username/your-repo
# PUBLIC_GISCUS_REPO_ID=your-repo-id
# PUBLIC_GISCUS_CATEGORY=General
# PUBLIC_GISCUS_CATEGORY_ID=your-category-id
